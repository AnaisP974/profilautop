{% extends 'base.html.twig' %}

{% block title %}CoverLetter{% endblock %}

{% block body %}
    <h1>CoverLetter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cover_letter.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ cover_letter.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ cover_letter.createdAt ? cover_letter.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ cover_letter.updatedAt ? cover_letter.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cover_letter_index') }}">back to list</a>

    <a href="{{ path('app_cover_letter_edit', {'id': cover_letter.id}) }}">edit</a>

    {{ include('cover_letter/_delete_form.html.twig') }}
{% endblock %}
