{% extends 'base.html.twig' %}

{% block title %}Statut Offre
{% endblock %}

{% block body %}
<main class="container">

	<h1 class="mb-4">Modifier le statut</h1>

	<p>Vous êtes sur le point de modifier le statut de l'annonce : "{{ job_offer.title }}"</p>
	{{ form_start(statusForm) }}
        <div class="m-2">
            {{ form_row(statusForm.status, {attr: {class: 'form-label select-form'}}) }}
        </div>

        <div class="d-flex justify-content-center w-100">
            {{ form_row(statusForm.submit, {attr: {class: 'btn btn-success mt-4'}}) }}
        </div>
	{{ form_end(statusForm) }}
</main>
{% endblock %}
