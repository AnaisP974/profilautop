<form method="post" action="{{ path('app_job_offer_delete', {'id': jobOffer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jobOffer.id) }}">
    <button class="btn btn-light border"><i class="bi bi-trash"></i></button>
</form>
